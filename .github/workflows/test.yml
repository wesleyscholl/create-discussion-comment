on: push
jobs:
  curl:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v1
    - name: test-action
      uses: ./workflows/actions/action
      with:
        args: echo curl -X POST https://httpbin.org/post
        
