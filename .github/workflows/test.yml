on: push
jobs:
  curl:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v1
    - name: test-action
      uses: wesleyscholl/create-discussion-comment@v1.0.1
      with:
        args: echo curl -X POST https://httpbin.org/post
        
